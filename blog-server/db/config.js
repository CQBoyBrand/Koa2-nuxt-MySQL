const config = {
    // 启动端口
    port: 3500,
    // 数据库配置
    database: {
        DATABASE: 'blog',
        USERNAME: 'brand',
        PASSWORD: '',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config