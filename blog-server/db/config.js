const config = {
    // 启动端口
    port: 3500,
    // 数据库配置
    database: {
        DATABASE: 'blog_test',
        USERNAME: 'root',
        PASSWORD: '你的数据库密码',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config